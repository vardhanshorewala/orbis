"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.contractsMeta = exports.ContractsMeta = void 0;
class ContractsMeta {
    constructor() {
        this.contractsMeta = new Map();
    }
    addressToKey(address) {
        return address.toString();
    }
    get(key) {
        return this.contractsMeta.get(this.addressToKey(key));
    }
    upsert(key, value) {
        const oldValue = this.contractsMeta.get(this.addressToKey(key));
        this.contractsMeta.set(this.addressToKey(key), { ...(oldValue ?? {}), ...value });
    }
    clear() {
        this.contractsMeta.clear();
    }
    delete(key) {
        this.contractsMeta.delete(this.addressToKey(key));
    }
}
exports.ContractsMeta = ContractsMeta;
exports.contractsMeta = new ContractsMeta();
