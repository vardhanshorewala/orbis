import { ExtendedTransaction, FlatTransaction } from '../test/transaction';
type PrettifiedProps = {
    failReason?: FailReason;
    from?: string;
    to?: string;
    on?: string;
    op?: string;
};
export type PrettyTransaction = Omit<FlatTransaction, keyof PrettifiedProps> & PrettifiedProps;
export type FailReason = {
    message: string;
};
export declare function extractFailReason(tx: FlatTransaction): FailReason | undefined;
export declare function prettifyTransaction(tx: ExtendedTransaction): PrettyTransaction;
export {};
