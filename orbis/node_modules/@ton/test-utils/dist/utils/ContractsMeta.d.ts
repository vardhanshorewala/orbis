import { Address, ContractABI } from '@ton/core';
export type ContractMeta = {
    wrapperName?: string;
    abi?: ContractABI | null;
    treasurySeed?: string;
};
export declare class ContractsMeta {
    protected contractsMeta: Map<string, ContractMeta>;
    private addressToKey;
    get(key: Address): ContractMeta | undefined;
    upsert(key: Address, value: Partial<ContractMeta>): void;
    clear(): void;
    delete(key: Address): void;
}
export declare const contractsMeta: ContractsMeta;
