import type { Loc, MappingInfo } from "./mapping";
import type { StackElement } from "./logs";
import type { FuncSourceLoc, FuncMapping } from "./func-mapping";
/**
 * Describes a single step in the trace.
 */
export type Step = {
    readonly loc: Loc | undefined;
    readonly instructionName: string;
    readonly stack: readonly StackElement[];
    readonly gas: number;
    readonly gasCost: number;
    readonly funcLoc: undefined | FuncSourceLoc;
};
/**
 * Describes the entire trace.
 */
export type TraceInfo = {
    readonly steps: readonly Step[];
};
/**
 * Creates a trace from the logs and mapping.
 */
export declare const createTraceInfo: (logs: string, mapping: MappingInfo, funcMapping: undefined | FuncMapping) => TraceInfo;
export declare const createTraceInfoPerTransaction: (logs: string, mapping: MappingInfo, funcMapping: undefined | FuncMapping) => TraceInfo[];
